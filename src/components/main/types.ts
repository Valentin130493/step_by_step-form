export type UserInfoFormValues = {
  phone: string;
  firstName: string;
  lastName: string;
  gender: string;
};
export type JobInfoFormValues = {
  address: string;
  workplace: string;
};
export type LoanInfoFormValues = {
  address: string;
  workplace: string;
};
